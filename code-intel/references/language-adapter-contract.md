# Language Adapter Contract

An adapter declares language identity, file extensions, AST support, candidate LSP commands, supported LSP capabilities, fallback commands, and fixture expectations.

Adapters are data-only in the MVP. They do not install tools or embed language-specific deep logic.
